#include <stdio.h>

int main(){
  int spe_num, fst_num;
  scanf("%d%d", &spe_num, &fst_num);
  int sum = 0;
  while(scanf("%d", num) != EOF){
    if (num == spe_num)
      sum += 2000000;
    else if (num == fst_num)
      sum += 200000;
  }
  
  return 0;
}
